import React, { useEffect } from 'react';\nimport { Provider as PaperProvider } from 'react-native-paper';\nimport { NavigationContainer } from '@react-navigation/native';\nimport { createBottomTabNavigator } from '@react-navigation/bottom-tabs';\nimport HomeScreen from './src/screens/Home';\nimport AddMileageScreen from './src/screens/AddMileage';\nimport ServicesScreen from './src/screens/Services';\nimport { initDB } from './src/db';\nimport { requestAndSchedule } from './src/utils/notifications';\n\nconst Tab = createBottomTabNavigator();\n\nexport default function App() {\n  useEffect(() => {\n    (async () => {\n      await initDB();\n      await requestAndSchedule(); // ask permissions and schedule initial notifications\n    })();\n  }, []);\n\n  return (\n    <PaperProvider>\n      <NavigationContainer>\n        <Tab.Navigator>\n          <Tab.Screen name="Home" component={HomeScreen} />\n          <Tab.Screen name="Add" component={AddMileageScreen} />\n          <Tab.Screen name="Services" component={ServicesScreen} />\n        </Tab.Navigator>\n      </NavigationContainer>\n    </PaperProvider>\n  );\n}